<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mainl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/soma_bg"
    android:fitsSystemWindows="true"
    tools:context="com.tac.Weathercast.activities.FirstActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="28dp"
        android:layout_centerVertical="true">

        <TextView
            android:id="@+id/hello"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins"
            android:text="HELLO THERE"
            android:textColor="@color/soma_text_muted"
            android:letterSpacing="0.22"
            android:textSize="11sp" />

        <ImageView
            android:id="@+id/pixelimage"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:layout_marginTop="28dp"
            android:src="@drawable/soma_wx_partly_day" />

        <TextView
            android:id="@+id/intro"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:fontFamily="@font/fraunces"
            android:gravity="center"
            android:text="Welcome to Weathercast"
            android:textColor="@color/soma_ink"
            android:textSize="26sp"
            android:lineSpacingExtra="2sp"
            android:letterSpacing="-0.02" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:fontFamily="@font/poppins"
            android:gravity="center"
            android:text="Your local sky, read at a glance — conditions, hourly and a seven-day view."
            android:textColor="@color/soma_text_secondary"
            android:textSize="13sp"
            android:lineSpacingExtra="3sp" />
    </LinearLayout>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/pixelbutton"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_alignParentBottom="true"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="28dp"
        android:background="@drawable/soma_button"
        android:fontFamily="@font/poppins"
        android:text="Get started"
        android:textAllCaps="false"
        android:textColor="@color/soma_on_fill"
        android:textSize="15sp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@id/pixelbutton"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:orientation="horizontal">

        <fr.castorflex.android.circularprogressbar.CircularProgressBar
            android:id="@+id/loading_spinner"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_gravity="center"
            android:layout_marginEnd="8dp"
            android:indeterminate="true"
            android:visibility="gone"
            app:cpb_color="@color/soma_ember"
            app:cpb_max_sweep_angle="300"
            app:cpb_min_sweep_angle="10"
            app:cpb_rotation_speed="1.0"
            app:cpb_stroke_width="2dp"
            app:cpb_sweep_speed="1.0" />

        <TextView
            android:id="@+id/progresstext"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins"
            android:text="Loading"
            android:textColor="@color/soma_text_muted"
            android:textSize="13sp"
            android:visibility="gone" />
    </LinearLayout>
</RelativeLayout>
